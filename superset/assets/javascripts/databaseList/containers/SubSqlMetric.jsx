import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default function SubSqlMetric () {
    return (
        <div className=" ">
            this is the SQL Metric.
        </div>
    );
}